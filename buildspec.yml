version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10  
    commands:
    - yarn install
    - yarn build
    - aws s3 sync build s3://sn-react-test --delete
    - aws s3 sync assets s3://sn-react-test/assets